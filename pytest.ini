[pytest]
addopts = -v -s --tb=short --html=reports/report.html --self-contained-html
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    smoke: quick checks for critical functionality
    regression: full suite for end-to-end coverage
    negative: tests for invalid inputs / error handling
